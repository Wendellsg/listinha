import { atom } from "jotai";

export const tokenAtom = atom(null);
export const userDataAtom = atom(null);
export const isLoadingAtom = atom(false);
export const userListsAtom = atom(null);
export const sugestionsAtom = atom(null);
